{
  "dependencies": {
    "@buxogabe/conclock": "^1.0.0",
    "bcrypt": "^5.1.1",
    "express": "^4.18.2",
    "sqlite3": "^5.1.6"
  },
  "scripts": {
    "build": "tsc --project tsconfig.build.json",
    "start": "node build",
    "test": "jest --silent --runInBand",
    "test-coverage": "jest --coverage --silent --runInBand",
    "dev": "concurrently \"npx tsc --project tsconfig.build.json --watch\" \"nodemon -q build\""
  },
  "devDependencies": {
    "@types/express": "^4.17.17",
    "@types/jest": "^29.5.4",
    "@types/node": "^20.6.0",
    "@types/sqlite3": "^3.1.8",
    "concurrently": "^8.2.1",
    "jest": "^29.7.0",
    "nodemon": "^3.0.1",
    "ts-jest": "^29.1.1",
    "typescript": "^5.2.2"
  }
}
